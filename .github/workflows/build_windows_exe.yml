name: build-desktop-packages

# 用途：云端自动打包 Windows exe + macOS 安装包（PyInstaller 不支持跨平台）
# 触发：手动（Actions 页面点 Run workflow）或推送 v* 标签
# 产物：Actions 运行页底部 Artifacts 下载 BizBot-windows.zip / BizBot-macos.zip

on:
  workflow_dispatch:
  push:
    tags: ["v*"]

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: 安装依赖
        run: |
          pip install -r requirements.txt
          pip install pyinstaller

      - name: 打包（单目录模式）
        run: pyinstaller salon_assistant.spec --noconfirm

      - name: 冒烟验证（exe 能启动且 API 就绪）
        shell: pwsh
        run: |
          $exe = Get-ChildItem "dist/BizBot/BizBot.exe"
          if (-not $exe) { throw "artifact not found: dist/BizBot/BizBot.exe" }
          # 启动 exe 并等待 API 就绪（120s 窗口，Windows Defender 首扫 517M 文件较慢）
          $p = Start-Process -FilePath $exe.FullName -ArgumentList "--port","18899","--username","smoke","--password","smoke123","--no-agent" -PassThru -RedirectStandardOutput smoke_out.log -RedirectStandardError smoke_err.log
          $ok = $false
          foreach ($i in 1..60) {
            Start-Sleep -Seconds 2
            try {
              $r = Invoke-WebRequest -Uri "http://127.0.0.1:18899/health" -UseBasicParsing -TimeoutSec 3
              if ($r.StatusCode -eq 200) { $ok = $true; break }
            } catch {}
          }
          Stop-Process -Id $p.Id -Force -ErrorAction SilentlyContinue
          if (-not $ok) {
            Write-Host "=== exe stdout (last 40 lines) ==="
            Get-Content smoke_out.log -ErrorAction SilentlyContinue | Select-Object -Last 40
            Write-Host "=== exe stderr (last 40 lines) ==="
            Get-Content smoke_err.log -ErrorAction SilentlyContinue | Select-Object -Last 40
            throw "exe started but API not ready in 120s"
          }
          Write-Host "OK: windows smoke passed"

      - name: 压缩产物
        shell: pwsh
        run: Compress-Archive -Path "dist/BizBot" -DestinationPath "BizBot-windows.zip"

      - uses: actions/upload-artifact@v4
        with:
          name: BizBot-windows
          path: BizBot-windows.zip
          retention-days: 30

  build-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: 安装依赖
        run: |
          pip install -r requirements.txt
          pip install pyinstaller

      - name: 打包（单目录 .app）
        run: pyinstaller salon_assistant.spec --noconfirm

      - name: 冒烟验证（可执行文件能启动且 API 就绪）
        run: |
          BIN="dist/BizBot/BizBot"
          [ -f "$BIN" ] || BIN=$(find dist -type f -perm +111 | grep -v "\.so" | head -1)
          [ -n "$BIN" ] || { echo "未找到打包产物"; exit 1; }
          "$BIN" --port 18899 --username smoke --password smoke123 --no-agent &
          PID=$!
          OK=0
          for i in $(seq 1 30); do
            sleep 2
            if curl -sf http://127.0.0.1:18899/health >/dev/null 2>&1; then OK=1; break; fi
          done
          kill $PID 2>/dev/null || true
          [ "$OK" = "1" ] || { echo "启动后 API 未就绪"; exit 1; }
          echo "✓ macOS 冒烟通过"

      - name: Ad-hoc 签名（免费；2026新规：无签名结构的可执行文件会被Gatekeeper判"已损坏"）
        run: |
          codesign --force --deep --sign - "dist/BizBot" 2>/dev/null || \
          find dist -name "*.app" -exec codesign --force --deep --sign - {} \; || true

      - name: 制作 .dmg 安装镜像
        run: |
          hdiutil create -volname "BizBot AI店长助手" -srcfolder "dist/BizBot" -ov -format UDZO BizBot-macos.dmg
          ls -lh BizBot-macos.dmg

      - name: 压缩产物（含双击启动器）
        run: |
          cp deploy/启动店长助手.command "dist/BizBot/启动店长助手.command" || true
          cd dist && zip -qry "BizBot-macos.zip" "BizBot" && cd ..
          ls -lh BizBot-macos.zip

      - uses: actions/upload-artifact@v4
        with:
          name: BizBot-macos
          path: |
            BizBot-macos.dmg
            BizBot-macos.zip
          retention-days: 30

  # 打 tag (v*) 时自动创建 GitHub Release 并挂安装包附件，用户在 Releases 页直接下载
  # 手动 workflow_dispatch 运行只验证打包，不发布（无 tag 自动跳过本 job）
  release:
    needs: [build-windows, build-macos]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          merge-multiple: true
      - uses: softprops/action-gh-release@v2
        with:
          files: |
            BizBot-windows.zip
            BizBot-macos.dmg
            BizBot-macos.zip
          generate_release_notes: true
